name: C/C++ CI

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Install compiler
        run: sudo apt-get update && sudo apt-get install -y g++

      - name: Compile all solutions
        run: |
          set -e
          for file in codeforces_practice/rate800/*.cpp; do
            echo "Compiling $file"
            g++ -std=gnu++17 -O2 -Wall -Wextra "$file" -o /tmp/a.out
          done
